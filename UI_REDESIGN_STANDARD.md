# Zephyr Hub 页面 UI 重构实施规范

## 1. 目的

本文是 Zephyr Hub 后续页面 UI 重构的强制实施与验收规范，用于约束从原型分析、代码实现到视觉验收和提交的完整过程。

本规范解决的核心问题是：不能只迁移原型的颜色和视觉语言，而应在保留真实业务能力的前提下，准确实现每个页面自己的结构、布局、尺寸、主题和交互。

本文不定义任何单一页面的具体布局。管理首页中的图表结构、卡片尺寸、Radar 排列、栏目增删和间距数值均属于管理首页自身要求，不得直接推广到账号管理、号池配置、日志、资源管理或其他页面。

## 2. 适用范围

本规范适用于：

- 管理后台页面。
- 用户控制台页面。
- 登录页和公开工具页。
- 页面使用的弹窗、抽屉、下拉菜单和局部交互组件。
- 暗色、亮色及响应式版本。

纯服务端接口、数据库和无视觉输出的业务逻辑不属于本规范的视觉验收范围，但 UI 重构不得改变它们的既有行为。

## 3. 规则优先级

出现冲突时，按以下顺序执行：

1. 用户对当前页面提出的明确要求。
2. 当前页面对应的暗色和亮色原型。
3. 当前项目的真实业务流程、权限、接口和数据含义。
4. 已确认的项目设计系统与公共组件规范。
5. 当前页面的旧 DOM、旧样式和旧组件结构。

旧实现只作为业务行为和数据来源参考，不得因为复用方便而覆盖当前页面原型。

如果原型与真实业务冲突，必须保留真实业务，并在实施前明确记录差异及处理方式。未经确认，不得自行删除、合并、改名或新增业务功能。

## 4. 规范用语

- **必须**：不满足则页面不能验收。
- **应当**：默认执行，只有明确技术或业务原因才能偏离。
- **可以**：根据页面实际情况选择。

## 5. 规则分类与作用域

所有新要求和修改意见必须先归类，不能直接写入全局样式。

| 类型 | 示例 | 作用域 |
| --- | --- | --- |
| 设计系统规则 | 色彩、字体、基础间距、通用按钮 | 全站 |
| 公共组件规则 | Tab、选择器、弹窗、表格基础样式 | 使用该组件的页面 |
| 页面布局规则 | 某页列数、卡片比例、区域顺序 | 当前页面 |
| 业务显示规则 | 邮箱不截断、状态文案、字段显隐 | 对应业务页面或组件 |
| 页面临时修正 | 为匹配单个原型增加的特殊尺寸 | 当前页面，合并前复查 |

页面专属规则只有满足以下任一条件时，才可以升级为公共规则：

- 项目设计规范明确将其定义为公共模式。
- 至少两个页面原型采用相同结构和交互。
- 升级后不会迫使其他页面偏离各自原型。

## 6. 页面合同

每个页面开始编码前，必须先建立页面合同。页面合同可以写入任务说明、页面专项文档或对应测试配置，但内容必须可追踪。

页面合同至少包含以下内容：

```md
# 页面名称

- 目标路由：
- 源码入口：
- 暗色原型：
- 亮色原型：
- 基准视口：375 / 1440 / 1920
- 依赖接口：
- 允许修改的业务范围：

## 页面结构

1. 标题区
2. 操作区
3. 主内容区
4. 弹窗或浮层

## 数据映射

| 原型字段 | 真实字段 | 空值处理 |
| --- | --- | --- |

## 交互

- 点击：
- 筛选：
- 刷新：
- 提交：
- 复制：

## 必须覆盖的状态

- 正常数据
- 加载中
- 空数据
- 接口错误
- 权限不足
- 长文本
- 大量数据

## 页面专属规则

-

## 已确认的原型差异

-
```

页面合同未记录的页面专属判断，不得自动推广为设计系统规则。

## 7. 实施流程

### 7.1 阶段一：原型盘点

编码前必须完成：

1. 确认目标路由及准确的暗色、亮色原型文件。
2. 使用相同浏览器和基准视口打开原型。
3. 记录关键区域的 DOM 层级、顺序、宽高和间距。
4. 列出原型中的控件、交互和页面状态。
5. 将原型字段映射到真实接口和业务行为。
6. 标记公共组件、页面专属组件和不可修改的业务逻辑。

完成页面合同前，不应开始正式样式实现。

### 7.2 阶段二：结构实现

本阶段只解决页面骨架：

1. DOM 层级必须与原型的信息层级一致。
2. 区域顺序、网格列数和主要比例必须与原型一致。
3. 先使用真实组件边界，再处理细节样式。
4. 现有组件结构与原型冲突时，应当调整组件或建立页面专属组件。
5. 禁止通过隐藏元素、负边距或大量定位来掩盖结构差异。

结构完成后必须进行第一次截图对照。结构未通过，不进入视觉细化阶段。

### 7.3 阶段三：视觉实现

1. 必须优先使用已确认的设计 Token。
2. 通用控件必须优先使用现有公共组件。
3. 页面专属布局必须放在页面或组件作用域内。
4. 禁止为了单个页面修改无作用域的全局选择器。
5. 必须逐项对照字体、行高、间距、尺寸、圆角、边框、背景、阴影和状态色。
6. 暗色与亮色必须同步实现，不能完成暗色后再根据印象推导亮色。
7. 原型中的交互光效或动效必须按其真实行为实现，不得只复制静态外观。

视觉主体完成后必须进行第二次截图和 DOM 尺寸对照。

### 7.4 阶段四：业务接入

1. 必须使用真实接口、真实字段和真实状态。
2. 不得为了匹配原型伪造生产统计数据。
3. 必须保留现有路由、权限、提交载荷和操作流程。
4. 必须补齐加载、空数据、错误、禁用、提交中和权限不足状态。
5. 必须检查长邮箱、长名称、长错误信息、大量表格行和动态数字。
6. 动态内容不得导致控件、网格或页面发生无意义跳动。

业务接入完成后必须进行功能回归和第三次视觉对照。

### 7.5 阶段五：响应式与主题验收

每个页面至少验证以下组合：

| 视口 | 暗色 | 亮色 |
| --- | --- | --- |
| `375px` | 必须 | 必须 |
| `1440px` | 必须 | 必须 |
| `1920px` | 必须 | 必须 |

存在断点特殊布局时，还应验证断点前后各一个尺寸。

必须检查：

- 页面没有异常横向溢出。
- 复杂表格只在自己的容器中滚动。
- 文本不遮挡其他内容。
- 控件不会因为动态文案改变固定布局。
- 弹窗和下拉菜单不超出视口。
- 键盘焦点清晰，图标按钮具有可访问名称。
- `prefers-reduced-motion` 和 `prefers-reduced-transparency` 行为正确。

## 8. 原型对照方法

原型与项目页面必须在相同条件下比较：

- 相同浏览器内核。
- 相同视口宽高。
- 相同设备缩放比例。
- 相同主题。
- 相同滚动位置。
- 尽可能稳定且等价的测试数据。

每次对照至少输出：

1. 原型截图。
2. 项目实现截图。
3. 页面整体宽度和高度。
4. 标题区、操作区、主网格、关键卡片或表格的 DOM 尺寸。
5. 控制台错误和横向溢出检查结果。

关键容器边界与原型的尺寸差异原则上不得超过 `2 CSS px`。受字体渲染或动态内容影响的区域可以存在少量差异，但不能产生肉眼可见的层级、对齐或换行偏差。

截图相似不能替代 DOM 尺寸检查，DOM 尺寸接近也不能替代实际截图检查。

## 9. 公共组件约束

### 9.1 复用原则

- 复用应当减少真实重复，不能以牺牲原型准确性为代价。
- 页面第一次出现的新模式，默认先作为页面专属实现。
- 确认复用后再提取公共组件，避免过早抽象。

### 9.2 修改原则

修改公共组件前必须：

1. 搜索全部使用位置。
2. 记录可能受影响的页面。
3. 判断修改应为默认行为、可选变体还是页面局部覆盖。
4. 对受影响页面执行最小视觉回归。

页面特有的宽度、排列和字段显隐不得通过修改公共默认值实现。

### 9.3 CSS 原则

- 公共 Token 只能表达稳定的设计语义。
- 公共组件样式必须由组件类名约束。
- 页面布局必须由页面根类名约束。
- 禁止使用脆弱的深层后代选择器修补旧结构。
- 禁止反复提高选择器权重解决样式冲突。
- 发现旧样式串入时，应定位所有权并消除冲突来源。

### 9.4 弹窗、抽屉与遮罩动效

所有新建弹窗、抽屉和遮罩必须使用公共层动效，不得在页面中重新定义一套时长或缓动曲线。

- Vue 条件层使用 `<Transition name="hub-layer">`。
- 弹窗内容使用 `hub-layer-panel`。
- 右侧抽屉内容同时使用 `hub-drawer-panel`。
- 管理弹窗和抽屉的根容器继续使用 `admin-modal-backdrop` 或 `log-drawer-backdrop`。
- 原生确认对话框继续使用 `confirm-dialog`，公共 CSS 已包含进入和退出状态。
- 公共动效 Token 位于 `app/assets/css/design-tokens.css`，实现位于 `app/assets/css/v2.css`。
- `prefers-reduced-motion: reduce` 时必须自动降级为无位移、近乎即时的状态切换。

页面可以调整尺寸、布局和内容，但不能覆盖公共层的基础时长、距离和缓动，除非当前原型明确要求不同的动效并完成影响评估。

## 10. 业务与原型的边界

原型负责定义视觉结构和交互表达，项目代码负责提供真实业务行为。两者不一致时按以下方式处理：

- 原型有展示但接口无数据：显示真实空状态，不伪造数据。
- 接口有重要字段但原型未展示：先确认是否隐藏，不自行删除。
- 原型交互缺少业务步骤：保留必要步骤，并按原型视觉补充。
- 原型操作会破坏权限或安全边界：保持现有权限和确认机制。
- 原型使用固定示例文案：替换为真实数据后重新检查布局。

## 11. 禁止事项

- 禁止把“按照原型”降级为只匹配配色和材质。
- 禁止从管理首页推断其他页面的列数、卡片比例或信息取舍。
- 禁止把某个页面的用户反馈直接写成全局规则。
- 禁止在缺少页面合同的情况下自行合并或删除信息。
- 禁止为了保留旧 DOM 而使用大量 CSS 覆盖模拟新结构。
- 禁止只验证默认桌面暗色状态。
- 禁止使用理想化短文本代替真实长文本测试。
- 禁止以类型检查和构建通过代替视觉验收。
- 禁止以截图接近代替功能和交互回归。

## 12. 完成定义

一个页面只有同时满足以下条件才可以标记为完成：

- 页面合同完整且与当前实现一致。
- 页面结构和信息层级与该页面原型一致。
- 关键区域经过浏览器 DOM 尺寸实测。
- 暗色和亮色均完成截图对照。
- 桌面、宽屏和移动端均无异常溢出或重叠。
- 加载、空数据、错误、长文本和大量数据状态完整。
- 真实接口、权限和原有操作流程没有回归。
- 页面专属样式没有泄漏到其他页面。
- 类型检查、相关测试和生产构建通过。
- 用户要求的最终视觉结果已经落实。

未满足其中任意一项时，只能标记为进行中，不能以“基础样式已完成”代替页面完成。

## 13. 提交规范

- 一个页面的 UI 重构应形成独立提交。
- 公共设计系统和公共组件修改应单独提交。
- 测试或视觉基线可以单独提交。
- 提交中不得混入无关业务改动。
- 提交前必须运行 `git diff --check`、类型检查和相关测试。
- 页面通过完整验收后再进行生产构建和最终提交。

建议提交格式：

```text
feat(ui): redesign account management
feat(ui): redesign upstream settings
fix(ui): align account management with prototype
test(ui): add account management visual coverage
```

## 14. 执行结论

后续 UI 重构必须以“单页原型合同”为执行单位。公共设计系统负责保持产品的一致性，但不能代替各页面自己的原型；现有实现负责提供真实业务，但不能限制新页面结构。

正确顺序固定为：原型盘点、结构实现、视觉实现、业务接入、响应式与主题验收、功能回归、独立提交。任何页面都不得跳过原型对照和浏览器实测。
