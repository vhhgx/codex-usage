# Zephyr Hub 聚合网关产品与技术方案

## 开发状态（2026-07-30）

状态口径：`[x]` 表示代码已实现并有当前工作区的自动化或构建证据；`[ ]` 表示仍需
外部环境、真实凭据或生产流量才能完成，不能仅凭本地实现判定完成。

- [x] PostgreSQL、Redis、MinIO/S3、Drizzle 迁移、管理员登录、服务端会话和加密基础设施。
- [x] 最小生产镜像定义、无 `drizzle-kit` 运行时迁移器、内部依赖 readiness 和香港私有全栈 Compose。
- [x] 渠道、模型映射、价格表、Hub Key、到期/权限/周期额度和一次性明文 Key 管理。
- [x] 六个约定的 OpenAI 兼容端点、统一错误和请求 ID、SSE 与 multipart 透传。
- [x] 优先级/加权轮询、并发控制、故障转移、健康检查、熔断和半开探测。
- [x] 加密正文归档、请求/尝试日志、小时/日聚合、保留期清理和 Redis 对账。
- [x] Key/渠道删除锁与历史汇总匿名合并，删除管理对象不会丢失长期统计或产生唯一键冲突。
- [x] Redis 共享登录/查询限流、可信代理地址判定、归档 JSON 认证字段脱敏和管理筛选输入校验。
- [x] 总览、Hub Keys、渠道、模型价格、请求日志、设置和原账号余量管理页面。
- [x] 管理变更与审计同事务提交；代理头净化、真实首字节、流式归档内存上限和历史日汇总缺陷已修复。
- [x] Prometheus 指标、签名 Webhook 告警、冷却/恢复通知和渠道端点被动健康观测。
- [x] Hub Key 凭据轮换/吊销、非流式 POST 幂等重放、单请求 Token/费用/图片保护。
- [x] Redis 共享流量排空、readiness 联动、运维令牌接口、PostgreSQL/MinIO 备份校验与恢复演练。
- [x] 审计查询页面和永久日汇总 CSV/JSON 导出。
- [x] 单元、生产构建及本地基础设施 E2E：故障注入、SSE、multipart、迁移、并发额度、归档脱敏/清理、共享限流、轮换、幂等、告警、排空、审计导出和历史汇总均通过。
- [ ] 通过已安装的 OpenAI Docs MCP 对 Responses 与 Images 当前 OpenAPI 字段做最终规范核对；需重启 Codex 后执行。
- [ ] 使用真实 CPA 与 Sub2API 凭据完成影子环境验证；`npm run test:hub-shadow` 已就绪。
- [ ] 在生产 HTTPS、真实对象存储生命周期和目标网络中完成长时间压测与正式切流。

当前实现状态不改变下文的目标设计；未勾选项仍属于发布前验收范围。

## 1. 总体方案

将现有 Nuxt 4 配额面板升级为 OpenAI 兼容聚合网关：

- 保留现有 CPA、CPA Manager Plus、Sub2API 配额查询能力。
- 新增 `/v1/**` 数据面，用户只使用 Hub Key，不接触上游 Key。
- 支持多个 CPA/Sub2API 渠道，并按模型配置优先级、加权轮询和故障转移。
- 管理员创建、停用和限额 Hub Key，查看全站及单 Key 统计、日志和完整请求响应。
- 第一版面向单管理员、中小规模部署，继续使用 Nuxt/Nitro，不拆分独立网关服务。

## 2. 系统架构

```text
OpenAI SDK / Codex / Client
          |
       Hub Key
          |
    Nuxt Nitro /v1/**
          |
  鉴权 -> 限额 -> 模型路由 -> 调度/熔断
          |
   +------+------+
   |             |
 CPA 渠道     Sub2API 渠道
   |
请求日志、统计、完整正文
   |
PostgreSQL + Redis + MinIO/S3
```

- PostgreSQL：管理员、渠道、模型映射、Hub Key、价格、请求元数据、统计聚合、审计日志。
- Redis：RPM、并发计数、额度预占、加权轮询状态、渠道健康和熔断状态。
- MinIO/S3：加密保存请求正文、响应正文、上传图片和生成图片，生命周期为 30 天。
- 详细请求元数据保留 1 年；小时/日聚合永久保留，用于年度和历史总量统计。

## 3. OpenAI 兼容接口

第一版实现以下端点，并透传 OpenAI 常用参数、错误格式和请求 ID：

- `GET /v1/models`
- `POST /v1/chat/completions`，包含 SSE 流式响应
- `POST /v1/responses`，包含流式响应和 image generation tool
- `POST /v1/embeddings`
- `POST /v1/images/generations`
- `POST /v1/images/edits`，支持 multipart 上传

`/v1/models` 返回所有当前存在健康渠道的公开模型。每个渠道维护上游模型名、Hub 模型名和支持端点；不支持对应端点的渠道不会进入候选集。

代理层移除用户 `Authorization`、`Host`、`Content-Length`、`Forwarded`、`X-Forwarded-*`、`X-Real-IP` 和 `Idempotency-Key` 等敏感、连接相关或可伪造请求头，注入所选渠道的上游 Key，并统一返回 `x-request-id`。

## 4. 渠道与调度

渠道支持添加多个 CPA/Sub2API 实例，每个渠道配置：

- 名称、类型、Base URL、加密凭据、启停状态。
- 优先级、权重、请求超时、最大并发。
- 支持模型、模型别名、支持端点和价格倍率。
- 主动健康检查与手动测试。

模型池可选择“优先级”或“加权轮询”策略，两种策略均启用故障转移：

1. 过滤停用、熔断、不支持模型/端点和已达并发上限的渠道。
2. 按策略选择首个渠道。
3. 在收到响应头前遇到连接失败、超时、`429` 或可重试 `5xx` 时切换下一渠道。
4. 流式响应开始后不得切换，以免产生拼接响应。
5. 网络状态不明确、可能已被上游计费的图片请求不自动重试。
6. 连续失败达到阈值后熔断，冷却期后半开探测；成功后恢复。

## 5. Hub Key 管理

Key 使用 `zh-` 前缀加高熵随机值，只在创建或轮换时显示完整值；数据库在独立凭据表保存带服务端 Pepper 的 HMAC、前缀和末四位。一个逻辑 Key 可保留多个凭据版本，轮换可立即切换或设置最长 7 天的重叠期；非当前凭据可独立吊销，并记录到期时间和最后使用时间。

每个 Key 支持：

- 名称、备注、启用/停用。
- 绝对到期时间或“创建后 N 天到期”，包括 7 天后自动失效。
- 允许模型和端点白名单。
- RPM、最大并发。
- 请求次数、Token、金额的总额度及日/周/月周期额度。
- 单次请求最大预计 Token、最高预计费用、最大图片数，以及允许的图片规格和质量。
- 自定义价格倍率。

每次请求入口都原子检查启用状态与到期时间；到期 Key 立即返回 OpenAI 格式 `401`。后台任务只负责将过期状态归档和释放缓存，权限判断不依赖定时任务。

金额按 Hub 价格表计算，分别配置输入、输出、缓存、推理和图片规格价格。请求前在 Redis 原子预占预计最大费用，结束后按上游 `usage` 或规范化用量结算并释放差额；RPM、并发和额度更新使用 Redis Lua 保证并发一致性，PostgreSQL 定期对账。

非流式 POST 支持 `Idempotency-Key`。网关以 Hub Key、端点、幂等键哈希和正文哈希锁定请求，
成功结果从加密归档重放；键相同但正文不同、原请求仍在处理或上游结果不明确时返回 `409`，
避免有费用副作用的请求被危险重试。幂等记录保留 7 天；流式请求不接受幂等键。

## 6. 日志与统计

每个请求记录：

- 请求 ID、Hub Key、时间、端点、请求模型和最终上游模型。
- 渠道、调度尝试、故障转移原因。
- 输入/输出/缓存/推理 Token、图片数量与规格、Hub 计费金额。
- 首字节时间、总延迟、HTTP 状态、流式状态、错误摘要、客户端 IP 哈希。
- 加密正文对象地址、内容哈希和正文过期时间。

完整请求和响应仅单管理员可查看。记录前必须删除 `Authorization`、Cookie、上游凭据等认证信息；提示词和模型输出按需求完整保留。对象采用 AES-256-GCM 信封加密，同时启用 S3 服务端加密和 30 天生命周期。SSE 响应先写临时文件并流式加密上传，仅在内存保留最多 4 MiB 用量解析尾部，避免长流响应耗尽进程内存。

统计接口支持：

- 全部时间、今日、滚动 24 小时、本周、本月、本年、自定义起止时间。
- 按 Hub Key、模型、渠道、端点、状态分组和过滤。
- 请求数、成功率、Token、费用、平均/P95 延迟、故障转移次数。
- 小时粒度用于 24 小时和短期查询，日粒度用于月、年和历史查询。
- 元数据保留期内可查询原始日志；超过保留期后继续使用永久日汇总提供长期统计和导出。

## 7. 管理后台

采用安静、紧凑的运维控制台布局，新增：

- 总览：总量卡片、趋势图、模型/渠道分布、错误率和渠道健康。
- Hub Key：创建、复制一次、限额、到期时间、模型权限、停用和用量详情。
- 渠道：CPA/Sub2API 配置、模型映射、优先级、权重、健康和测试。
- 请求日志：组合筛选、分页、请求详情、调度过程、完整正文查看。
- 价格表：模型 Token 与图片规格价格、启用时间。
- 系统设置：时区、保留期、熔断和默认超时。
- 告警与流量：当前告警、签名 Webhook 测试、共享流量排空和活动请求计数。
- 审计：按管理员、动作、目标和时间查询，查看变更详情。
- 用量导出：按 Key、渠道、模型、端点、状态和时间下载 CSV/JSON 日汇总。
- 保留现有账号余量页面，改为管理员登录后可见。

单管理员由环境变量在首次启动时初始化，密码使用 Argon2id 保存；登录采用 Redis 中的服务端会话和 `HttpOnly + Secure + SameSite=Strict` Cookie。所有渠道、Key、价格和日志查看操作写入审计日志。

## 8. 数据模型与实现组件

核心表为：

- `admin_users`
- `channels`
- `channel_models`
- `hub_keys`
- `hub_key_credentials`
- `key_model_rules`
- `model_prices`
- `request_logs`
- `request_attempts`
- `usage_rollups`
- `audit_logs`
- `idempotency_records`

采用 Drizzle ORM 管理 PostgreSQL 迁移，`ioredis` 管理实时状态，AWS S3 SDK 同时兼容 MinIO。上游凭据和正文对象使用独立主密钥加密，主密钥只来自服务端环境变量。

现有 `/api/usage/query`、CPA/Sub2API 配额解析器和账号余量组件继续复用；新增管理 API 全部位于 `/api/admin/**`，公共代理仅位于 `/v1/**`。

## 9. 实施顺序

1. [x] 引入 PostgreSQL、Redis、MinIO、迁移、管理员登录和加密基础设施。
2. [x] 完成渠道、模型映射、价格表和 Hub Key 管理。
3. [x] 实现统一代理、OpenAI 错误格式、流式透传、限流和额度结算。
4. [x] 加入优先级、加权轮询、健康检查、熔断和故障转移。
5. [x] 完成日志正文归档、小时/日聚合及清理任务。
6. [x] 重构后台导航并实现总览、Key、渠道、日志和统计页面。
7. [x] 修复自检缺陷并完成可观测性、告警、Key 轮换、幂等和单请求成本保护。
8. [x] 完成流量排空、备份恢复演练、审计查询和用量导出。
9. [ ] 在影子环境接入一个真实 CPA 与一个真实 Sub2API，验证后再切换正式客户端 Base URL。

## 10. 测试与验收

- [x] Hub Key 创建后可调用全部约定端点，过期、停用、超模型范围和超额度请求均被拒绝。
- [x] 并发请求下 RPM、并发和额度不会因竞争条件绕过限制。
- [x] 优先渠道失败时正确切换；上游返回响应头或流式响应开始后不发生二次路由。
- [x] Chat Completions、Responses 的 SSE 内容和结束事件不被缓冲或破坏。
- [x] Images multipart、生成结果和大正文可正确转发、加密归档并在 30 天后删除。
- [x] 每笔成功或失败请求都能关联适用的 Key、模型、渠道、Token、费用、时间和调度尝试。
- [x] 今日与滚动 24 小时结果不同且准确；保留期内周/月/年/自定义区间与原始日志抽样一致，保留期后与永久日汇总一致。
- [x] 渠道凭据、Hub Key 全文和认证请求头不会出现在数据库明文、应用日志或浏览器响应中。
- [x] Key 轮换重叠/吊销、幂等重放/正文冲突和 Token/图片/费用单请求上限均通过 E2E。
- [x] 指标鉴权、签名 Webhook、流量排空/readiness、审计查询和 CSV/JSON 导出均通过 E2E。
- [x] 备份 SHA-256 与 PostgreSQL dump 校验、隔离数据库和 MinIO 对象恢复演练通过。
- [x] 完成单元、集成、并发、故障注入、SSE、multipart、数据库迁移和生产构建测试。

## 11. 假设与默认值

- 默认时区为 `Asia/Shanghai`，后台允许修改。
- 第一版只有单管理员，不提供用户注册、充值、套餐或 Key 持有人自助查询。
- 默认策略为优先级路由加故障转移；管理员可按模型池切换为加权轮询。
- 实施时需要对照最新 OpenAI 官方 OpenAPI 规范再次验证 Images 和 Responses 字段；上述端点名称作为第一版固定兼容范围。
- 当前会话已安装 `openaiDeveloperDocs` MCP，但工具需要重启后加载，因此此项仍保持未完成状态。
